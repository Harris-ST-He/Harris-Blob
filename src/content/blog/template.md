---
# 文章标题，会显示在文章页和列表页
title: 这里写文章标题
# 文章摘要，会显示在文章列表和文章详情页开头
description: 用一句话概括这篇文章想说什么。
# 发布时间
pubDate: 2026-04-11
# 标签，会显示在文章页和归档页
tags:
  - 标签一
  - 标签二
# 是否为草稿；写作时建议先用 true，准备发布再改成 false
draft: true
---

这里开始写正文。

## 小标题示例

先写一小段引子。

- 要点一
- 要点二
- 要点三

如果这篇文章准备发布：

1. 把 `title` 改成正式标题
2. 把 `description` 改成最终摘要
3. 把 `pubDate` 改成当天日期
4. 把 `draft: true` 改成 `draft: false`
